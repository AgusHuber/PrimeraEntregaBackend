<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestión de Carritos</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>

        body {
            background-color: #f8f9fa; 
        }

        .container {
            max-width: 900px; 
        }

        h1 {
            color: #28a745; 
            text-align: center;
            margin-bottom: 40px;
        }

        .cart-list {
            margin-top: 20px;
        }

        .cart-item {
            background-color: #ffffff;
            padding: 15px;
            margin-bottom: 10px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .cart-item .btn {
            margin-left: 10px;
        }

        .btn-success {
            font-size: 16px;
        }

        .btn-create {
            width: 100%;
            font-size: 16px;
            padding: 12px;
            border-radius: 8px;
        }

        .btn-create:hover {
            background-color: #218838;
        }

        .cart-item-title {
            font-weight: bold;
            color: #333;
        }

        .cart-item-actions {
            display: flex;
        }

        .cart-item-actions button {
            margin-left: 10px;
        }
    </style>
</head>
<body>

    <div class="container mt-5">
        <h1>Gestión de Carritos</h1>

        <div id="cartList" class="cart-list">

        </div>

        <button onclick="createCart()" class="btn btn-success btn-create mt-4">Crear Nuevo Carrito</button>
    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/cartClient.js"></script>

</body>
</html>